## ADDED Requirements

### Requirement: Create-style scaffolder package

The workspace SHALL provide a create-style scaffolder package that can create a Baresync starter monorepo from an empty target directory.

#### Scenario: Scaffolder creates starter workspace

- **WHEN** a user runs the create package with a project name
- **THEN** it SHALL create a root workspace containing `apps/app`, `apps/server`, and `packages/sync-contract`
- **AND** the generated workspace SHALL include root scripts for sync artifact generation, local migration generation, server migration generation, and development startup

#### Scenario: Core package remains separate

- **WHEN** the scaffolder package is added
- **THEN** the existing `baresync` package SHALL remain the library and `generate`/`doctor` CLI package
- **AND** scaffolder-specific prompt and template dependencies SHALL NOT be required by normal `baresync` library consumers

### Requirement: Package manager detection

The scaffolder SHALL detect the invoking package manager and use it consistently in proxied commands and generated instructions.

#### Scenario: Package manager detected from user agent

- **WHEN** `process.env.npm_config_user_agent` identifies Bun, pnpm, npm, or Yarn
- **THEN** the scaffolder SHALL use that package manager without prompting the user

#### Scenario: Bun runtime fallback

- **WHEN** no package-manager user agent is present but the script is running under Bun
- **THEN** the scaffolder SHALL use Bun as the package manager

#### Scenario: Unknown package manager fallback

- **WHEN** the package manager cannot be detected
- **THEN** the scaffolder SHALL prompt the user to select Bun, pnpm, npm, or Yarn

### Requirement: Official CLI proxying

The scaffolder SHALL proxy official app framework CLIs interactively instead of maintaining local copies of their app templates.

#### Scenario: Tauri CLI receives terminal control

- **WHEN** the scaffolder creates `apps/app`
- **THEN** it SHALL run the official Tauri app creation command with inherited stdio so the user can answer native Tauri prompts

#### Scenario: Server CLI receives terminal control

- **WHEN** the scaffolder creates `apps/server`
- **THEN** it SHALL run the selected official Hono or Elysia creation command with inherited stdio so the user can answer native server prompts

#### Scenario: Failed proxied command stops scaffold

- **WHEN** a proxied official CLI exits with a non-zero code
- **THEN** the scaffolder SHALL stop and report which setup step failed

### Requirement: Todo sync-contract template

The scaffolder SHALL generate a default todo sync-contract package.

#### Scenario: Todo contract files generated

- **WHEN** the scaffolder writes `packages/sync-contract`
- **THEN** it SHALL create package metadata, TypeScript config, sync config, generate script, local synced schema, API synced schema, local runtime schema, API runtime schema, and constants

#### Scenario: Todo contract demonstrates relationship ordering

- **WHEN** a user inspects the generated sync config
- **THEN** it SHALL include `lists` and `todos` tables
- **AND** `todos` SHALL reference `lists`
- **AND** both tables SHALL use `scope_id` as the scope column

#### Scenario: Generated artifacts are not hand-written

- **WHEN** the sync-contract template is copied
- **THEN** generated sync artifacts SHALL NOT be hard-coded into the template
- **AND** the generated workspace SHALL provide a script that runs Baresync artifact generation from the sync config

### Requirement: Migration generation scripts

The scaffolder SHALL configure schema-first migration generation and SHALL NOT copy static SQL migration files.

#### Scenario: Local migration config generated

- **WHEN** the scaffolder configures the Tauri app
- **THEN** it SHALL generate a Drizzle config that reads local sync-contract schemas and writes local SQLite migrations to the path consumed by the Tauri plugin

#### Scenario: Server migration config generated

- **WHEN** the scaffolder configures the server app
- **THEN** it SHALL generate a Drizzle config that reads API sync-contract schemas and writes server SQLite migrations to the server migration directory

#### Scenario: SQL migrations generated by user command

- **WHEN** the scaffold completes
- **THEN** SQL migration files SHALL be generated only when the user runs the documented migration generation scripts

### Requirement: Server integration modules

The scaffolder SHALL generate Baresync server integration in separate modules and minimally mount those modules into recognized server entrypoints.

#### Scenario: Hono route module generated

- **WHEN** the user selects Hono
- **THEN** the scaffolder SHALL generate a Baresync sync route module exposing `/push`, `/pull`, and `/status` routes
- **AND** the generated server entrypoint SHALL mount those routes under `/sync` when the entrypoint shape is recognized

#### Scenario: Elysia route module generated

- **WHEN** the user selects Elysia
- **THEN** the scaffolder SHALL generate a Baresync sync route module exposing `/push`, `/pull`, and `/status` routes
- **AND** the generated server entrypoint SHALL mount those routes under `/sync` when the entrypoint shape is recognized

#### Scenario: Unrecognized server entrypoint fallback

- **WHEN** the scaffolder cannot safely patch the server entrypoint
- **THEN** it SHALL leave the generated entrypoint intact
- **AND** it SHALL print manual mount instructions for the generated sync route module

### Requirement: Tauri integration modules

The scaffolder SHALL generate Tauri-side Baresync integration using compact plugin registration and framework-neutral JS helpers.

#### Scenario: Rust plugin setup generated

- **WHEN** the scaffolder configures the Tauri app
- **THEN** it SHALL add Baresync Rust dependencies and generate or patch Rust setup that registers the Baresync plugin through the simplified builder API
- **AND** the setup SHALL use generated contract metadata instead of app-authored table order

#### Scenario: Frontend helper modules generated

- **WHEN** the scaffolder configures frontend source files
- **THEN** it SHALL generate framework-neutral DB and sync-client helper modules
- **AND** it SHALL NOT modify framework-specific root components or provider trees

### Requirement: Scaffold completion guidance

The scaffolder SHALL print next steps that match the detected package manager and generated scripts.

#### Scenario: Completion output includes generation commands

- **WHEN** scaffolding completes
- **THEN** the final output SHALL include commands to install dependencies, generate local migrations, generate server migrations, generate sync artifacts, and start development
