# baresync

SQLite-first sync infrastructure for Tauri apps.

This package provides the schema helpers, sync generator, local DB helpers,
server helpers, and Tauri client used by Baresync consumers.

See the repository root README for the full integration guide and examples.
